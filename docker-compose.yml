version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://api-eng.ngocthang.io.vn/api
    container_name: english-learning-frontend
    ports:
      - "5173:80"
    restart: always
